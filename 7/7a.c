#include <lpc21xx.h>

int main() {
    int a = 6, b = 2;
    int sum, sub, mul, div;

    sum = a + b;     // Addition
    sub = a - b;     // Subtraction
    mul = a * b;     // Multiplication
    div = a / b;     // Division

    return 0;
}
